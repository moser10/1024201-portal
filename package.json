{
  "name": "1024201-portal",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy && node scripts/post-deploy.mjs",
    "setup": "npm install && npm link ./cli",
    "cli": "node cli/bin/1024.js",
    "cli:link": "npm link ./cli"
  },
  "devDependencies": {
    "wrangler": "^4.107.1"
  },
  "engines": {
    "node": ">=18"
  }
}
