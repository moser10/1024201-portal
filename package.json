{
  "name": "1024201-portal",
  "private": true,
  "scripts": {
    "deploy": "npx wrangler deploy && node scripts/post-deploy.mjs",
    "cli": "node cli/bin/1024.js",
    "cli:link": "npm link ./cli"
  }
}
